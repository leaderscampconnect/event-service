server:
  port: 8081

spring:
  application:
    name: event-service
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/event_db}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
